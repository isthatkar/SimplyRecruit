import { ThemeProvider } from "@mui/material";
import React from "react";
import Theme from "../Styles/Theme";

const Projects = () => {
  return <ThemeProvider theme={Theme}>Aplication view page</ThemeProvider>;
};

export default Projects;
